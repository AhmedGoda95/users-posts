import { Box } from "@mui/material";

const Footer = () => {
  return (
    <Box
      sx={({}) => ({
        backgroundColor: "#2E88C4",
        height: 50,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#fff",
        fontSize: 18,
      })}
    >
      Footer
    </Box>
  );
};

export default Footer;
